# Copy this file to ".env" and fill in your deployed counter worker URL.
# See worker/README.md for how to deploy the worker and get this URL.
# This value is NOT secret (the browser calls it directly); it's an env var
# only so it's easy to change without editing source.
VITE_COUNTER_URL=
